# Same project as the web app (anon key — never commit service_role here)
SUPABASE_URL=https://xxxx.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Comma-separated origins for CORS (browser must send Authorization)
CORS_ORIGIN=http://localhost:5173

# API listen
PORT=8787
